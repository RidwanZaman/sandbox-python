#Classic loading screen
import time
import sys
Loading = "."
print("Loading ", end="", flush=True)
while True:
   time.sleep(1)
   sys.stdout.write("
   if len(Loading) > 3:\b"*len(Loading))
   Loading += "."
       Loading = "."
   print(Loading, end="", flush=True) 

